package com.example.planwithfriends.ui.theme

import androidx.compose.ui.graphics.Color

// --- PRIMĂVARĂ (Spring) ---
val SpringPrimary = Color(0xFFB8DB80)    // Bara de jos (Verde deschis)
val SpringBackground = Color(0xFFF7F6D3) // Fundal (Gălbui)
val SpringTertiary = Color(0xFFFFE4EF)   // Carduri (Roz deschis)
val SpringSecondary = Color(0xFFF39EB6)  // Buton + (Roz)

// --- VARĂ (Summer) ---
val SummerBackground = Color(0xFFFFEDCE) // Fundal (Galben cald)
val SummerTertiary = Color(0xFFFFC193)   // Carduri (Portocaliu deschis)
val SummerTertiaryDark = Color(0xFFFF6D00)
val SummerSecondary = Color(0xFFFF8383)  // Buton + (Coral)
val SummerSecondaryDark = Color(0xFFFF5050)
val SummerPrimary = Color(0xFFFF3737)    // Bara de jos (Roșu)

// --- TOAMNĂ (Autumn) ---
val AutumnSecondary = Color(0xFFB77466)  // Buton + (Cărămiziu)
val AutumnBackground = Color(0xFFFFE1AF) // Fundal (Bej luminos)
val AutumnTertiary = Color(0xFFE2B59A)   // Carduri (Maro deschis)
val AutumnPrimary = Color(0xFF957C62)    // Bara de jos (Maro închis)

// --- IARNĂ (Winter) ---
val WinterPrimary = Color(0xFF52357B)    // Bara de jos (Mov închis)
val WinterSecondary = Color(0xFF5459AC)  // Buton + (Albastru-Mov)
val WinterTertiary = Color(0xFF648DB3)   // Carduri (Albastru)
val WinterBackground = Color(0xFFB2D8CE) // Fundal (Bleu deschis)

// --- DARK MODE (Păstrat pentru modul întunecat) ---
val AppGreenDark = Color(0xFF81C784)
val AppPinkDark = Color(0xFFF06292)
val AppCalendarDarkPink = Color(0xFFEE6895)
val AppBackgroundDark = Color(0xFF121212)
val AppSurfaceDark = Color(0xFF1E1E1E)